# frozen_string_literal: true

RSpec.describe Jekyll::Semantic do
  it "has a version number" do
    expect(Jekyll::Semantic::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
